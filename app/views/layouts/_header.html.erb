<header>
  <nav>

    <div class="top-nav-container group">
      <!-- <h1 class="logo"><a href="<%= root_url %>">PicsCasa</a></h1> -->
      <h1 class="logo"><a href="/#/">Photo191</a></h1>
      <h1 class="underlogo"><a href="/#/">Share beautiful pictures</a></h1>

 
      <ul class="top-right-nav group">
        <% if signed_in? %>
        <!-- <li>
          <a href="<%= notifications_url %>">Notifications <span class="badge"><%= current_user.notifications.unread.count %></span></a>
          <ul class="notif-dropdown">
            <% if current_user.notifications.unread.count == 0 %>
              <li>No new notifications</li>
            <%else%>
              <%= render partial: "notifications/notification", collection: current_user.notifications.unread %>

            <% end %>
          </ul>
        </li> -->
          <!-- .limit(5)  -->
        <li class="user-dropdown">
          <img class="avatar nav-avatar" src="<%= current_user.avatar.url(:thumb) %>">
          <ul class="user-dropdown-items">
            <li><strong><%= current_user.email %></strong></li>
            <li></li>
            <li><a href="<%= edit_user_url(current_user) %>">Account</a></li>
            <li><%= button_to "Logout", session_url, method: :delete %></li>
          </ul>
        </li>

        <% else %>
        <li><a href="<%= new_user_url %>">Create Account</a></li>
        <li><a href="<%= new_session_url %>">Sign In</a></li>
        <% end %>
      </ul>
    </div>

    <div class="bottom-nav-container group">
      <ul class="middle-nav group">
        <li><a href="/#/albums/public">Photo Feed</a></li>
        <% if signed_in? %>
        <li><a href="/#/albums">Albums</a></li>
        <li><a href="/#/photos">Photos</a></li>
        <li><a href="/#/tags">Tags</a></li>
        <% end %>


        <% if signed_in? %>
        <li><a href="/#/albums/new">New Album</a></li>
        <% end %>
      </ul>
    </div>
  </nav>



</header>